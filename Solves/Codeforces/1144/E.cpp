#include <bits/stdc++.h>
using namespace std;

const int MAXN = 2e5 + 5;

char s[MAXN], t[MAXN];

int main() {
	int n; scanf("%d %s %s", &n, s, t);

	ans = (s + t)/2
	for (int i = n-1; i >= 0; i--) {
		
	}
}