#include <bits/stdc++.h>

using namespace std;

struct aten {
	int ini, fim;
};

int main() {
	int n, ans = 0;
	scanf("%d", &n);
	aten vec[n];

	for (int i = 0; i < n; i++) {
		scanf("%d %d", &vec[i].ini, &vec[i].fim);
	}

	for (int i = 0; i < n; i++) {
		
	}

	return 0;
}