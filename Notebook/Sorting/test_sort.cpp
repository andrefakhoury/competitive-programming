#include <bits/stdc++.h>

using namespace std;
const int MAXN = 100005;

int n, v[MAXN];

//The standart std::sort() has complexity O(N logN)

int main() {
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> v[n];
	}

}